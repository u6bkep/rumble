[alias]
xtask = "run --package xtask --"
# To iterate against vendor/damascene instead of the git-pinned rev, use the
# wrapper at `scripts/damascene-local.sh`. It applies `.cargo/local-damascene.toml`
# as a `--config` overlay and restores Cargo.lock afterwards so the lockfile
# always reflects the git pin (CI and other machines stay unaffected).
