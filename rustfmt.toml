imports_granularity = "Crate"
format_strings = true
max_width = 120
# Vendored crates have their own workspace and formatting conventions. The
# rumble workspace already excludes them, but `cargo fmt --all` walks into
# path dependencies anyway — list them here so rustfmt itself skips them.
ignore = ["vendor"]