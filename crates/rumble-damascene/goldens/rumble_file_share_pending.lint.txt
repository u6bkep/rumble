TextOverflow node=root.group.0.group.1.group.0.virtual_list[chat:history].group.5.group.1.group[chat:file-card:demo-pending].group.2.group.1.text.0 chat/file_card.rs:282 :: nowrap text exceeds its box by X=61; use .ellipsis(), wrap_text(), or a wider box
HitOverflowCollision node=root.group.0.group.1.group[chat-sidebar:resize] src/app.rs:488 :: expanded hit targets for keys `chat:history` and `chat-sidebar:resize` overlap by 1x610px — hit-test resolves the collision by paint order, so `chat-sidebar:resize` owns that invisible band. Reduce `.hit_overflow(...)`, add real gap/padding, or make one visible row/control own the full intended target.
HitOverflowCollision node=root.group.0.group.1.group.0.group.4.group.1.icon_button[chat:share-file] src/chat.rs:590 :: expanded hit targets for keys `chat:input-row` and `chat:share-file` overlap by 34x1px — hit-test resolves the collision by paint order, so `chat:share-file` owns that invisible band. Reduce `.hit_overflow(...)`, add real gap/padding, or make one visible row/control own the full intended target.
HitOverflowCollision node=root.group.0.group.1.group.0.group.4.group.1.icon_button[chat:paste-image] src/chat.rs:594 :: expanded hit targets for keys `chat:input-row` and `chat:paste-image` overlap by 34x1px — hit-test resolves the collision by paint order, so `chat:paste-image` owns that invisible band. Reduce `.hit_overflow(...)`, add real gap/padding, or make one visible row/control own the full intended target.
HitOverflowCollision node=root.group.0.group.1.group.0.group.4.group.1.icon_button[chat:sync] src/chat.rs:598 :: expanded hit targets for keys `chat:input-row` and `chat:sync` overlap by 34x1px — hit-test resolves the collision by paint order, so `chat:sync` owns that invisible band. Reduce `.hit_overflow(...)`, add real gap/padding, or make one visible row/control own the full intended target.
HitOverflowCollision node=root.group.0.group.1.group[chat-sidebar:resize] src/app.rs:488 :: expanded hit targets for keys `chat:input-row` and `chat-sidebar:resize` overlap by 1x52px — hit-test resolves the collision by paint order, so `chat-sidebar:resize` owns that invisible band. Reduce `.hit_overflow(...)`, add real gap/padding, or make one visible row/control own the full intended target.
