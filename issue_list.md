# issues

- frequent popping on incoming audio.
- link cleaner bot shows up as a user in the root room. this kind of bot should be text-only and not show up as a user.
- disconnect SFX should mirror the connect SFX, not be a sawtooth.
- mute and unmute SFX only play every other press of the preview button.
- self voice light sticks on after un muting until the first time you speak.

# features

- show probabliy of speach in VAD setting. maybe add a generic way for a processing step to report a value that can be shown in the UI.