target/
dist/
.idea/
vendor/
data/
certs/
.venv/
chat_logs/
client_configs/
reference/
.claude/worktrees/

# Generated artifacts from `cargo run -p rumble-damascene --bin dump_bundles`
crates/rumble-damascene/out/

# Generated wasm admin UI bundle (built by tools/build_admin_web.sh). The page
# shell (web-dist/index.html) is tracked; the compiled bundle is not.
crates/server/web-dist/pkg/

# Local-only patch override for iterating against vendor/damascene; invoked via
# `scripts/damascene-local.sh` (see .cargo/config.toml).
.cargo/local-damascene.toml

# Bridge persistent Ed25519 identity (default path; private key, 0600).
bridge-identity.key
