target/
dist/
.idea/
vendor/
data/
certs/
.venv/
chat_logs/
client_configs/
reference/
.claude/worktrees/

# Generated artifacts from `cargo run -p rumble-aetna --bin dump_bundles`
crates/rumble-aetna/out/

# Local-only patch override for iterating against vendor/aetna; invoked via
# `cargo aetna-local …` (see .cargo/config.toml).
.cargo/local-aetna.toml
