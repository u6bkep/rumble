# Copy to `.env` and edit. Consumed by docker-compose.yml.

# Public domain clients connect to and that your Let's Encrypt cert is issued
# for. The bridge also dials the server by this name internally.
RUMBLE_DOMAIN=voice.example.com
